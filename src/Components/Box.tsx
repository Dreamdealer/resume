import styled from 'styled-components';

const Box = styled.div`
    display: flex;
    flex-grow: 0;
    flex-shrink: 0;
    flex-direction: row;
    justify-content: center;
    align-items: stretch;
`;

export default Box;
